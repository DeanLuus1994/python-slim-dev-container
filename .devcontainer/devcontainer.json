{
	"name": "Python 3 & PostgreSQL",
	"dockerComposeFile": "docker-compose.yml",
	"service": "app",
	"workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
	"features": {
		"ghcr.io/devcontainers/features/docker-in-docker:2": {
			"version": "latest",
			"moby": false
		},
		"ghcr.io/devcontainers/features/common-utils:2": {
			"installZsh": true,
			"installOhMyZsh": true,
			"upgradePackages": false
		}
	},
	"remoteUser": "vscode",
	"hostRequirements": {
		"gpu": "optional"
	},
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.python",
				"ms-python.vscode-pylance",
				"ms-python.black-formatter",
				"ms-python.flake8",
				"charliermarsh.ruff",
				"ms-python.mypy-type-checker"
			],
			"settings": {
				"python.defaultInterpreterPath": "/usr/local/bin/python",
				"python.linting.enabled": true,
				"editor.formatOnSave": true,
				"editor.codeActionsOnSave": {
					"source.organizeImports": "always"
				}
			}
		}
	},
	"postCreateCommand": "git clone ${ZSH_AUTOSUGGESTIONS_REPO:-https://github.com/zsh-users/zsh-autosuggestions.git} ${ZSH_CUSTOM:-~/.oh-my-zsh/custom}/plugins/zsh-autosuggestions && echo 'plugins=(git docker docker-compose zsh-autosuggestions)' >> ~/.zshrc && echo '\\n\\n======================================================' && if [ \"$DEV_MODE\" = \"true\" ]; then echo '🚀 CONTAINER INITIALIZED IN DEV MODE'; else echo '🚀 CONTAINER INITIALIZED IN PROD MODE'; fi && echo 'Environment variables loaded:' && echo '- POSTGRES_USER: '$POSTGRES_USER && echo '- POSTGRES_DB: '$POSTGRES_DB && echo '- STORAGE: '$STORAGE && echo '======================================================\\n\\n'"
}